import 'package:flutter/material.dart';

abstract class NEColors {
  static const Color kWhite = Colors.white;
  static const Color kBlack = Colors.black;
  static const Color kClear = Colors.transparent;

  static const Color kElectricCyan = Color.fromRGBO(0, 255, 217, 1.0);
  static const Color kMidnightTeal = Color.fromRGBO(2, 27, 25, 1.0);
}
